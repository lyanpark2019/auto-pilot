{
  "_doc": "Headless prompt is static (load-only). Must preserve literal HEADLESS markers + AskUserQuestion ban + bash ${...} expansion verbatim.",
  "prompt": "headless",
  "call": "load",
  "expects_substring": [
    "HEADLESS MODE",
    "auto-pilot server",
    "HARNESS_HEADLESS",
    "Never call AskUserQuestion",
    "stdin is /dev/null",
    "${HARNESS_HEADLESS:-0}"
  ],
  "expects_not": ["{iter_n}", "{phase}", "auto-pilot-iter:"]
}
