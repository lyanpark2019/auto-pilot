# Module size budget — documented exceptions to the ≤500-line rule.
# Format: <path>|<max_lines>   (lines starting with # ignored)
# Enforced by scripts/quality/check-module-size.sh (wired into CI).
# Add an entry ONLY with a justified reason in the comment above it.
#
# Vendored Codex-fork CLI (codex/ tree = repo-SoT, one-way synced out via
# sync-to-codex.sh — spec "codex" subsystem row). Single-file by upstream fork
# layout; splitting would diverge the synced surface. 907 lines at import
# (7a6a6b3); cap 920 = small headroom only, not license to grow — split it if
# it needs substantive new code.
codex/skills/migrate-to-codex/scripts/cli.py|920
#
# SKILL.md files are prose interface specs read by the agent in one pass;
# splitting them across files would break the single-load contract and the
# relative scripts/references refs. adversarial-review-loop SKILL.md grew to
# 625 when lifecycle mode (the absorbed pm-quality-harness-loop orchestrator)
# was folded in 2026-06-07. (setup-harness SKILL.md is exactly 500 == the
# default limit, so it needs no exception.)
skills/adversarial-review-loop/SKILL.md|640
#
# test_hooks_guards.py accumulates parametrized matrix tests for all hook
# guards (branch-lock, gh-auth, reviewer-write, worker-scope-gate); the
# table-driven test pattern is the right shape here — splitting would
# scatter the per-hook contract tables across files.
tests/test_hooks_guards.py|640
#
# orchestrator.py is the single composition root for all CLI subcommands; it
# cannot be split without introducing circular-import risk (all cmd_* functions
# share STATE_DIR, _emit, _warn helpers from the same module). Slice C adds
# cmd_ledger_append + cmd_ledger_rebalance (thin glue; all logic in _ledger.py)
# + phase-end try/except wiring (~60 lines). fix/loop-residuals adds
# cmd_resume (~22 lines, cap-recovery); duplicate approved-count loop removed
# (gate_phase_end now returns the count directly). Slice RES1 adds 2 lines
# (_recover.register_cli_subparsers wiring; all logic in _recover.py).
# Phase-2 learnings mirror adds 2 lines (_mirror_learnings.register_cli_subparsers
# for improvements-mirror subcommand; all logic in _mirror_learnings.py).
# Phase-2a enrich-persist adds 2 lines (_enrich_persist.register_cli_subparsers
# for enrich subcommand; all logic in _enrich_persist.py).
# inc3 Phase 1 adds 2 emit_escalation call sites (doom-loop + evidence_failed,
# ~25 net lines) + datetime import; all logic delegated to _escalation_emit.py.
# inc3 Phase 3 adds 2 measure_escalation wiring lines (all logic in measure_escalation.py).
# round-budget handler (_emit_hard_stop + cmd_round_budget) extracted to _round_budget.py
# via register_cli_subparsers pattern; 2 wiring lines replace ~45 inline lines.
# Cap 640 = 631 current + 9 headroom (freed headroom is intentional runway).
scripts/orchestrator.py|640
#
# test_headless_loop.py is the integration test suite for headless-loop.py; all
# tests share loop_module/state_dir fixtures and the _args helper, so splitting
# would duplicate fixture overhead or require a conftest. OBS3 adds TestWallClock
# (3 tests) + TestUsageLedger (2 tests) = +85 lines. Cap 520 = 516 + 4 headroom.
tests/test_headless_loop.py|520
#
# test_state_write_guard.py is a table-driven script-style test for the
# state-write-guard hook; CASES + EXTRA_CASES lists cannot be split without
# duplicating the run_case helper or adding a conftest. FIX1 + FIX2 additions
# (14 new bypass cases) grew it past 500. inc3-Phase2 adds 2 cases for the
# escalation-resolver role. Cap 540 = 534 current + 6 headroom.
hooks/test_state_write_guard.py|540
#
# _worktree.py gained atomicity helpers (FIX 2: _force_remove_worktree_and_branch +
# try/except rollback in create) and idempotency probe (FIX 1: _token_already_in_log +
# already_applied branch in apply_to_main). The module cannot be split because all
# methods share WorktreeManager state and the PatchResult / ApplyResult ADTs.
# Cap 520 = 506 current + 14 headroom.
scripts/_worktree.py|520
#
# test_worktree.py is a table-driven integration test suite for WorktreeManager;
# all tests share _init_repo / _fake_contract / _commit_one helpers + the
# _ready_to_apply / _brownfield_repo fixtures. Splitting would duplicate fixture
# overhead or require a conftest import chain. FIX 1/2/3 tests added 150 lines
# (4 new test functions). Cap 660 = 644 current + 16 headroom.
tests/test_worktree.py|660
#
# test_reviewer_wrapper.py grew past 500 from prior worktree changes (not audit-r2-fix
# scope); table-driven matrix tests for ReviewerWrapper dispatch; cannot split without
# duplicating the mock-factory helpers. Cap 520 = 510 current + 10 headroom.
tests/test_reviewer_wrapper.py|520
#
# headless-loop.py is the single-process headless driver; all loop_iteration /
# _accumulate_usage / _early_exit_status / _handle_timeout helpers share the
# module-level ROOT/STATE_FILE/LOG_DIR snapshot + the _budget seam, so splitting
# would fork the cwd-snapshot wiring. The state_transaction concurrency conversion
# (state.json RMW lock spanning load->mutate->write) added ~18 net lines across the
# 3 RMW spots. Cap 515 = 505 current + 10 headroom.
scripts/headless-loop.py|515
#
# test_ledger.py is the integration suite for _ledger.py; all tests share the
# tmp ledger / contracts-tree fixtures + the schema-valid record builders, so
# splitting would duplicate fixture overhead or require a conftest. The
# ledger_transaction concurrency conversion added a TestLedgerTransaction class
# (6 tests incl. a multiprocessing lost-update pin). The routing-ledger v2 contract
# role/task_class fields added TestContractRoleTaskClass (5 tests, +106 lines).
# Cap 750 = 739 current + 11 headroom.
tests/test_ledger.py|750
#
# check_doc_reference_integrity.py is the multi-form citation guard (path:line +
# path#token with identifier/filename/slug branches + asset-count + hook-count
# checkers). The slug normalization fix (P2 blind spot) added _normalize_anchor +
# expanded _check_anchor_citation to 3 branches. Cannot split without circular
# import (all helpers share _FileCache + _violation). Cap 515 = 512 + 3 headroom.
scripts/docs/check_doc_reference_integrity.py|515
#
# test_doc_reference_integrity.py accumulates tests for all citation forms
# (path:line, path#token identifier/filename/slug, asset-count, hook-count,
# scan-scope, symbol-proximity). The slug normalization tests added 3 new cases.
# Splitting would duplicate the _run helper + repo fixture or require a conftest.
# Cap 510 = 505 + 5 headroom.
tests/test_doc_reference_integrity.py|510
#
# test_mirror_learnings.py is the full acceptance suite for _mirror_learnings.py.
# The 16 Fix-1 / Fix-2 regression tests (prune-safety + sentinel strict ownership)
# share _valid_ticket / _write_ticket / render_gotcha helpers with the baseline
# tests; splitting would duplicate helper definitions or require a conftest.
# Fix-3 (section 13) adds 2 tests for the corrupt-ticket-blocks-prune invariant
# (88 lines). Cap 710 = 694 current + 16 headroom.
tests/test_mirror_learnings.py|710
#
# _escalation.py hosts both the enrichment-record writer (_record_enrichment) and the
# resolution writer (record_resolution) + cmd_escalation_resolve CLI handler. Splitting
# would force the CLI handlers to import each other's writers, creating circular-import
# risk — all cmd_* functions share _resolve_fp / _load_record / ledger_lock / validate_escalation
# from the same module. Cap 560 = ~550 current + ~10 headroom.
scripts/_escalation.py|560
#
# test_measure_enrich_precision.py is the full acceptance suite for
# measure_enrich_precision.py.  All tests share the _official / _community_two_corr /
# _community_single fixture builders; splitting would duplicate these helpers or
# require a conftest.  The measure_delta + --compare CLI tests add 241 lines
# (sections 14 + 15).  Cap 695 = 684 current + 11 headroom.
tests/test_measure_enrich_precision.py|695
#
# test_escalation.py is the full acceptance suite for _escalation.py; sections
# 1-6 (schema, fingerprint, RMW, acceptance, seam variants, byte-stability) plus
# the new section 7 (auto-abandon regression guards — 3 tests) share the same
# FakeFetcher / _obs / _valid_record / Observation helpers, so splitting would
# duplicate fixture overhead or require a conftest. Cap 550 = 540 + 10 headroom.
tests/test_escalation.py|550
#
# test_enrich_persist.py is the full acceptance suite for _enrich_persist.py;
# sections 1-12 share the _official / _community helpers + canonical_sha import,
# so splitting would duplicate helper definitions or require a conftest. The P2
# dry-run no-escalation test (section 6b) added ~55 lines. Cap 530 = 524 + 6 headroom.
tests/test_enrich_persist.py|530
#
# test_demote.py is the full acceptance suite for the downvote/reinstate path;
# all tests share _seed_promoted / _make_ledger_and_repo / _run helpers.
# The P1 watermark fix added TestReinstateWatermark (2 tests) + TestDownvoteStateGuard
# (3 tests) = +75 lines.  Splitting would duplicate helper definitions or require
# a conftest.  Cap 510 = 501 current + 9 headroom.
tests/test_demote.py|510
#
# test_learnings.py is the full acceptance suite for _learnings.py + snapshot
# integration.  The A/B gate-vs-ungated tests (select_tickets gated=False,
# resolve_learnings kill-switch) share _valid_ticket / _write_ticket / _bind_snapshot
# helpers with the baseline tests; splitting would duplicate helper definitions or
# require a conftest.  The cold-review opt-in env-parse fix added 5 new tests
# (=0/=false/=true/=yes/not-disabled-extended) = +76 lines.
# Cap 610 = 597 current + 13 headroom.
tests/test_learnings.py|610
