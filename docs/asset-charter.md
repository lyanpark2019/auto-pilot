---
type: charter
topic: asset-role-charter
source_commit: dd849c89c248f86591f5d70676df2dd23830642c
manual_edit: false
---

# Asset Role Charter

모든 plugin 자산의 역할 헌장 — 자산은 ≥1 pillar 에 역할 정의 의무, 역할-없는 자산 =
REMOVE/merge 후보. 자산 타입 정의의 SoT = `scripts/build_dashboard_data.py` →
`collect_assets()` (emitted types; 수기 타입 목록 금지). 매 라운드 charter↔collector
1:1 재검증 (drift = finding). Binding-contracts 색인: README "Binding contracts
inventory" 절 (상호링크 — 이 파일이 충전, README 는 링크만).

Pillars: ① 자율 코딩 루프 (PM-worker-이중리뷰) · ② 문서 신선도 (doc-management) ·
③ 지식 영속 (vault·retro·memory) · ④ 안전·집행 (hooks·contracts·gates).

Round-1 스냅샷 (113 assets, 1:1 vs collector @ 36e754e73e). Verdict 집행
(INTEGRATE/REMOVE) 후 rows 는 라운드마다 재생성된다 — 살아있는 표의 SoT 는
`.planning/score/asset-roles.md` (최신 라운드), 이 파일은 freshness-추적 영속본.

| Asset | Verdict | Role (1줄) | Pillar | Input→Output | Invocation | Overlap-free 근거 |
|---|---|---|---|---|---|---|
| agent:adr-audit | INTEGRATE | ADR factual-fidelity corrector — rewrites unsupported ADR sections to match raw source | ③ | meta/content-audit-rN.md ADR gaps + decisions/adr-*.md + raw/*.md → corrected ADRs + meta/adr-corrections.md + JSON summary | vault PM ticket on adr_fidelity dim flag (vault/sources/notebooklm.py:277 dispatch map; vault-pm-orchestrator.md:146,179) | NOT overlap-free — same cite/weaken/remove correction workflow as concept-grounding (different artifact only) → vault-knowledge-author |
| agent:adr-generator | INTEGRATE | ADR page generator from postmortem/decision raw content (Context/Decision/Consequences) | ③ | raw/<file>.md briefing+sources → <cat>/decisions/adr-NNN-*.md + decisions/_index.md TOC | vault PM ticket on adr_pages dim <10 (vault/sources/notebooklm.py:268) | overlaps concept-populator's node→page authoring pattern → vault-knowledge-author |
| agent:adversarial-auditor | INTEGRATE | Independent strict re-scorer of vault structural rubric (stricter offsets, non-deterministic sampling, distrust score-state.json) | ④ | vault path + vault/templates/rubric.yaml → meta/audit-rN.md (per-dim score + spot-checks + PASS/NEEDS-WORK verdict) | vault-pm-orchestrator every 2 rounds (agents/vault-pm-orchestrator.md:170) + /vault-audit command (→ vault-score mode per CEO) | NOT overlap-free — recomputes the same structural rubric as vault-score scripts; independence/red-team pattern duplicated by adversarial-review-loop → INTEGRATE into vault-score audit mode |
| agent:auto-pilot-claude-reviewer | CORE | Cold Claude reviewer inside auto-pilot contract loop — schema-valid review.json under 3-wall read-only sandbox | ①④ | CONTRACT_DIR ticket + worktree diff + verify.sh re-run → outputs/claude-reviewer/review.json + exit-code + done marker | PM loop dispatch per worker diff; health-probed at commands/auto-pilot.md:40 | sole cold-Claude reviewer (legacy claude-reviewer deleted 2026-06-07 — unification executed); shares review substance via review-core.md SoT, with unique contract-loop I/O (ticket SHA check, atomic JSON output, hook-enforced sandbox) — no other asset does sandboxed in-loop review |
| agent:auto-pilot-codex-reviewer | CORE | Codex CLI adversarial reviewer in contract loop with frozen-diff integrity check and hallucination sanity-check | ①④ | ticket(diff_path + diff_sha256) → codex exec --sandbox read-only → sanity-checked review.json | agents/pm-orchestrator.md:253 subagent_type dispatch in auto-pilot loop | sole Codex adversarial reviewer (legacy codex-adversarial deleted 2026-06-07 — unification executed); shares substance via review-core.md SoT, with unique diff-SHA freeze + JSON contract protocol + codex sandbox enforcement |
| agent:backlinks-enricher | INTEGRATE | Ensure ≥2 inbound wikilinks per source page via Related Sources sections | ③ | <cat>/sources/*.md inbound-link census → Related Sources sections (≤3 links/page) | vault PM ticket on backlinks dim <10 (vault/sources/notebooklm.py:273) | NOT — near-duplicate of orphan-linker/density-booster wikilink enrichment → vault-graph-enricher |
| agent:bases-creator | INTEGRATE | Generate Obsidian .base YAML dashboards (4 vault-root views + per-cat views) | ③ | vault frontmatter schema → *.base YAML files (filter/sort table views) | vault PM ticket on bases dim <5 (vault/sources/notebooklm.py:272) | only .base writer in the plugin, but single-shot structure task folded into vault-structure-curator per CEO |
| agent:claude-reviewer | DELETED | Cold Claude reviewer for adversarial-review-loop skill mode — inline-prompt dispatch, verbatim text verdict (legacy) | ① | PM-supplied diff + spec + contract scope + CLAUDE.md excerpts (inline) → text verdict (APPROVE/REJECT, findings table, verify re-run output) | (deleted) | superseded by hardened pair (ticket/frozen-diff/JSON), deleted 2026-06-07 — unification candidate executed |
| agent:code-perfector | KEEP | One-shot Python cleanup pipeline: dead code (vulture) + lint + readability + doc touch-up + commit/push | ① | any Python repo → categorized fixes + full verification report + commit (push gated on user approval) | user request ('코드 완벽하게 정리', 'dead code + lint 한번에') or proactive post-refactor | edges overlap adversarial-review-loop codebase mode (score→improve) and residue-audit (semantic dead code), but the one-shot mechanical sweep-to-commit pipeline is not offered by any other asset |
| agent:codex-adversarial | DELETED | Codex CLI adversarial second-opinion reviewer for skill-mode dual review — text verdict with hallucination sanity-check (legacy) | ① | diff + spec + CLAUDE.md excerpts → codex exec prompt → parsed findings → sanity-checked text verdict | (deleted) | superseded by hardened pair (ticket/frozen-diff/JSON), deleted 2026-06-07 — unification candidate executed |
| agent:community-labeler | INTEGRATE | Replace graphify 'Community N' placeholder labels with 2-5 word semantic names from member nodes | ③ | graph.json + .graphify_analysis.json → .graphify_labels.json ({community_id: label}) | vault PM ticket on label_fit flag / post-graphify cleanup (vault/scripts/ticket_system.py:179 example) | only semantic community-labeling worker, but single structure task folded into vault-structure-curator per CEO |
| agent:concept-grounding | INTEGRATE | Fix unsupported concept-page claims: add citation, weaken, or remove with audit-trail HTML comments | ③ | content-audit-rN.md hallucination list + concepts/*.md + raw/*.md → corrected pages + meta/concept-corrections.md + JSON | vault PM ticket on concept_accuracy flag (vault/sources/notebooklm.py:276) | NOT — same correction workflow as adr-audit (different artifact) → vault-knowledge-author |
| agent:concept-populator | INTEGRATE | Bootstrap concepts/ + entities/ pages from top-degree graph nodes (5-8 per type per cat) | ③ | graph.json + .graphify_labels.json → concept/entity pages with frontmatter + Related Nodes wikilinks | vault PM ticket on concept_entity_depth <10 (vault/sources/notebooklm.py:267) | authoring overlap with adr-generator's node→page pattern → vault-knowledge-author |
| agent:confidence-rebalancer | INTEGRATE | Re-classify edge confidence bands to rubric tolerance (EXT ≥10%, INF 40-80%, AMB ≤15%) with same-file co-occurrence honesty rule | ③ | per-cat graph.json edge confidences → promoted/demoted edges in place | vault PM ticket on confidence_balance flag (vault/sources/notebooklm.py:266) | NOT — operates on the same edge-confidence surface as the other 3 edge workers → vault-edge-curator |
| agent:content-fact-checker | KEEP | Read-only factual-grounding auditor: samples vault content (edges/concepts/ADRs/labels) against raw NotebookLM source fulltexts and emits correction tickets | ③④ | vault + raw/*.md + graph.json + labels → meta/content-audit-rN.md (scores + NO-support lists + tickets for PM) | PM content phase (agents/vault-pm-orchestrator.md:86,176) + /vault-content-verify (→ vault-score content mode per CEO) | no other asset grounds vault content against raw NotebookLM sources; doc-management audits a different corpus; adversarial-auditor checks structure not factual grounding; downstream correction agents depend on its audit  |
| agent:cross-cat-prefixer | INTEGRATE | Namespace-prefix node IDs per category and re-merge cross-category graph to eliminate ID collisions | ③ | per-cat graph.json → graph-prefixed.json → merged graph.json + graph.html + hyperedge restore | vault PM ticket on conflict_dup flag / post multi-cat ingestion (vault/sources/notebooklm.py:274) | only collision fixer, but single merge-pipeline step folded into vault-structure-curator per CEO |
| agent:cross-vault-linker | INTEGRATE | Build meta/cross-vault-links.md with verified relative wikilinks between sibling Obsidian vaults | ③ | sibling vault dirs + topic grep → meta/cross-vault-links.md (real/approx status table) | vault PM dispatch on cross_vault dim flag (vault/sources/notebooklm.py:269); stub placeholder seeded by vault/sources/code.py:110 | OVERLAPS the link-enrichment family (orphan-linker, backlinks-enricher) — only the cross-vault scope differs → INTEGRATE into vault-graph-enricher |
| agent:density-booster | INTEGRATE | Targeted INFERRED-edge enrichment for categories below 1.5 graph density | ③ | ticket cat targets + graph.json → INFERRED edges added to listed cats only | WORKER_FOR_DIM graph_density route (vault/sources/notebooklm.py:265) + vault-pm-orchestrator.md:132; fixture in vault/tests/test_cost_tracker.py:20 | NOT overlap-free — self-declared same algorithm as edge-enricher → INTEGRATE into vault-graph-enricher |
| agent:docs-verifier | REMOVE | Read-only cross-check of a docs-worker page against actual code (file:line, signatures, 100-pt rubric) | ② | ticket (target_page + scope_files) → stdout JSON score breakdown + CRITIQUE for re-dispatch | vault-pm-orchestrator code-area mode verification phase (vault-pm-orchestrator.md:24); listed in selftest.py:37 | OVERLAPS doc-management AUDIT mode and auto-pilot-claude-reviewer verification — doc-core supremacy → REMOVE |
| agent:docs-worker | REMOVE | Write one hallucination-free module doc page from a scope_files ticket contract | ② | ticket (scope_files + deliverable path + optional revise_critique) → wiki page + stdout report | vault/sources/code.py:154 plan_tickets emits worker_type="docs-worker" per category; selftest.py:37 expects it | OVERLAPS doc-management REBUILD authoring — doc-core supremacy → REMOVE |
| agent:drift-fixer | REMOVE | Update doc signatures/symbols to match current code for claim_drift/symbol_drift entries | ② | drift ticket (items[doc, doc_says, code_has]) → minimal-diff doc edits + .vault-builder/drift-fixer-actions.md + JSON receipt | vault/pipeline/fix.py:35-36 FIXER map (claim_drift/symbol_drift) dispatched from drift report (drift.py:67-68); tested in test_dispatch.py:23 | OVERLAPS doc-management MAINTAIN mode — doc-core supremacy → REMOVE |
| agent:edge-enricher | INTEGRATE | Global INFERRED-edge density boost (≥1.5 edges/nodes per cat) with confidence tiers + 1 hyperedge per cat | ③ | graph.json per cat → INFERRED edges (conf 0.55-0.85) + co_participates_in hyperedge | no code dispatch route found; only selftest.py frontmatter check — density-booster took its dim | NOT overlap-free — density-booster is the scoped variant of the SAME algorithm → INTEGRATE into vault-edge-curator |
| agent:edge-fact-corrector | INTEGRATE | Demote/remove edges flagged NO-support by content audit while preserving AMB ratio cap | ③ | meta/content-audit-rN.md + graph.json → demoted/removed edges + meta/edge-fact-corrections.md + JSON receipt | WORKER_FOR_DIM edge_fact route (vault/sources/notebooklm.py:275); fixture in vault/tests/test_worker_backup.py:16; score_content.py rerun path verified to exist | Overlaps edge-store curation family (edge-enricher/extracted-booster/confidence-rebalancer all mutate the same graph.json confidence fields) → INTEGRATE into vault-edge-curator |
| agent:extracted-booster | INTEGRATE | Promote grounded INFERRED edges (conf ≥0.75, token co-occurrence in one raw file) to EXTRACTED until 25-30% ratio | ③ | graph.json + raw/*.md → promoted edges capped at target ratio | no WORKER_FOR_DIM route; vault-pm-orchestrator.md:133 lists it as alternate for confidence_balance — ambiguous routing | NOT overlap-free — confidence-rebalancer holds the same dim in code; both mutate confidence fields → INTEGRATE into vault-edge-curator |
| agent:gap-filler | REMOVE | Create AST-grounded doc pages for code modules drift-detector found undocumented | ② | gap drift ticket (module + public_count) → docs/modules/<flattened>.md + actions log + JSON receipt | vault/pipeline/fix.py:33 FIXER map gap route; dispatched from drift report (drift.py:65); tested in test_dispatch.py:21 | OVERLAPS doc-management REBUILD page generation — doc-core supremacy → REMOVE |
| agent:goal-judge | REMOVE | Read-only skeptical gate for GoalBuddy phase/completion/parallel-safety decisions | ①(adjacent — external goalbuddy, out of plugin scope) | board state.yaml + receipts → single goalbuddy_receipt_v1 JSON decision | plain-name dispatch from external goalbuddy skill (README.md:28); zero in-plugin dispatch or tests | DUPLICATES global ~/.claude original — duplication itself is the defect → REMOVE plugin copy |
| agent:goal-scout | REMOVE | Read-only evidence mapper for one active GoalBuddy task (≤12 commands, ≤12 evidence items) | ①(adjacent — external goalbuddy, out of plugin scope) | active task + receipts → goalbuddy_receipt_v1 JSON (facts/contradictions/evidence) | plain-name dispatch from external goalbuddy skill (README.md:28); zero in-plugin dispatch or tests | DUPLICATES global ~/.claude original → REMOVE plugin copy |
| agent:goal-worker | REMOVE | Bounded writer executing exactly one reversible GoalBuddy work package within allowed_files | ①(adjacent — external goalbuddy, out of plugin scope) | task (allowed_files + verify + stop_if) → edits + verify run + goalbuddy_receipt_v1 JSON | plain-name dispatch from external goalbuddy skill (README.md:28); zero in-plugin dispatch or tests | DUPLICATES global ~/.claude original → REMOVE plugin copy |
| agent:harness-evaluator | CORE | Skeptical contract reviewer + post-implementation QA enforcing write-set compliance and hard-threshold criteria | ①④ | spec.md + sprint contract/handoff → contract sign-off/reject + {NN}-eval.md verdict + progress.json update | commands/harness-qa.md frontmatter agent dispatch + negotiation partner named in harness-build.md:3; documented in setup-harness SKILL.md:92 | Distinct from auto-pilot reviewers: those review diffs/tickets in the PM loop; evaluator negotiates pre-code contracts and exercises the RUNNING app (Playwright/hurl) — no other asset does contract-gate + live QA |
| agent:harness-generator | CORE | Build one sprint at a time against spec.md after evaluator-approved contract; self-test then hand off | ① | spec.md + progress.json → {NN}-contract.md → implementation commits → {NN}-handoff.md | commands/harness-build.md frontmatter agent dispatch; documented in setup-harness SKILL.md:92 | Differs from agent:worker — worker takes PM tickets in the autonomous loop; generator negotiates contracts with an evaluator in the file-handoff harness pattern (different protocol, different artifacts) |
| agent:harness-planner | CORE | Expand a 1-4 sentence product prompt into spec.md (features, sprint breakdown, AI-feature opportunities, budget.json) | ① | user prompt + CLAUDE.md/ADRs → .claude/harness/spec.md + progress.json init | commands/harness-plan.md frontmatter agent dispatch; documented in setup-harness SKILL.md:92 | Nothing else does prompt→spec expansion; pm-orchestrator plans tickets from existing goals, planner creates the product spec itself |
| agent:hot-cache-filler | INTEGRATE | Populate per-category hot.md (god nodes/bridges/source files/questions/cross-vault) from .graphify_analysis.json | ③ | .graphify_analysis.json + .graphify_labels.json → <cat>/hot.md with verified wikilinks | WORKER_FOR_DIM hot_cache route (vault/sources/notebooklm.py:270); placeholder sections seeded at vault/sources/code.py:106-109 | Within the graph-enrichment family (consumes same graphify artifacts as density/edge workers, writes cache instead of edges) → INTEGRATE into vault-graph-enricher |
| agent:orphan-linker | INTEGRATE | Vault wiki orphan-page fixer — adds inbound wikilinks from parent/index pages to 0-inbound pages | ③ | vault pages with 0 inbound wikilinks → wikilinks added from closest parent/category index | vault-pm-orchestrator ticket for the orphan-pages gap (manual table row agents/vault-pm-orchestrator.md:143; not in the notebooklm.py auto-plan map) | NOT overlap-free — same graph-link-density cluster as backlinks-enricher / density-booster / hot-cache-filler → CEO consolidation target vault-graph-enricher |
| agent:orphan-pruner | REMOVE | Doc dead-reference pruner — strikes/removes refs to deleted code files per drift report | ② | orphan-drift ticket items [{doc, ref}] → docs edited (refs removed/marked stale) + actions log JSON | vault-pm-orchestrator drift-fix mode via vault/pipeline/fix.py DRIFT_WORKER map ("orphan" → orphan-pruner) | OVERLAPS skill:doc-management MAINTAIN/AUDIT (dead-ref repair is exactly its job) — DOC-CORE SUPREMACY applies |
| agent:pm-orchestrator | CORE | PM contract for the autonomous loop — plan/critic-gate/dispatch/dual-review/verify/commit state machine the main session self-references | ①④ | spec + CLAUDE.md chain + state.json → contracts, parallel worker/reviewer dispatches, atomic commits, state transitions | Self-referenced by the main Opus session running skills/auto-pilot (SKILL.md:116 lists it as required reading); not dispatched as a subagent | Only PM contract for the coding loop; vault-pm-orchestrator is a separate (vault) domain; swarm PM lives in swarm/scripts/prompts — no duplication of this contract |
| agent:retro | CORE | Post-run retrospective analyst — mines state.json/logs for doom-loops and appends evidence-cited trap→consequence→guard lessons | ③ | .planning state + run artifacts/logs → append-only bullets in .claude/insights.md + short report | PM dispatches at phase end (pm-orchestrator.md:98) or user asks post-mortem ("what went wrong this run") | No other plugin asset writes lessons to the memory surface; reviewers gate diffs, retro gates nothing — orthogonal by design (explicit "NOT a reviewer" section) |
| agent:security-reviewer | MERGED → agent:review-gatekeeper (`security` mode) | MERGED 2026-06-07 — full OWASP-Top-10 trust-boundary substance preserved as review-gatekeeper's `security` mode (zero capability loss); pattern-list drift-risk fixed by consolidating the dispatch triggers into specialist-pool.md's mode-bearing routing table (now the single SoT) | — | — | — | superseded by agent:review-gatekeeper row |
| agent:specialist-pool | KEEP | Registry mapping changed-file patterns → specialist reviewers the PM co-dispatches with the default review pair | ① | diff file paths → list of specialist agents to add to the parallel review message + disagreement-handling policy | Read by PM during REVIEW FAN-OUT (pm-orchestrator.md hard rule 7); never dispatched itself | Partially overlaps pm-orchestrator.md (rule 7 cites it, no table restate) and security-reviewer.md's own pattern list (duplicated fact); table itself exists nowhere else — keep as the SoT for routing |
| agent:stub-merger | INTEGRATE | Vault stub consolidator — merges ≤2-source wiki stubs into their community parent page and deletes the stub | ③ | stub wiki articles + .graphify_labels.json → parent pages with appended subsections, stubs deleted | vault-pm-orchestrator ticket for wiki_articles dim (consolidation direction) | NOT overlap-free — inverse twin of wiki-stub-expander on the same wiki_articles dimension → both fold into vault-structure-curator |
| agent:swarm-explorer | KEEP | Read-only universal codebase mapper at swarm bootstrap — stack/commands/entry-points/CLAUDE.md hierarchy snapshot | ① | project tree → .planning/autopilot/knowledge/project-snapshot.md + project-files.json | PM spawns via Task tool during swarm bootstrap (run-pm.sh explorer step) | Only project-context analyst in the swarm path; setup-harness scans projects too but produces harness artifacts, not the swarm knowledge-dir contract |
| agent:swarm-monitor | KEEP | Live swarm health diagnostician — 8-check report over tmux panes + .planning/autopilot ledger/logs/queues | ① | tmux session state + .planning/autopilot/{logs,scores,ledger,inbox,...} → markdown health table + suggested actions | Task-spawned when swarm-status finds anomalies or user asks "is the swarm healthy / why did worker-N fail" | swarm-status skill is the shallow check; monitor is the only deep per-worker diagnostic — explicit two-tier split, no third overlapping asset |
| agent:swarm-verifier | KEEP | Independent second-opinion verifier after a merge verdict — re-runs acceptance, attempts adversarial reproductions, can downgrade verdict | ①④ | ticket + diff + score file → score file updated in place with verifier block (passed/reproductions/downgraded_to) | PM spawns via Task tool from swarm pm-verify prompt after a "merge" verdict | auto-pilot's codex/claude reviewers gate pre-merge in the contract loop; this is the swarm path's post-verdict falsification step — different pipeline, shared rubric file only |
| agent:tdd-enforcer | MERGED → agent:review-gatekeeper (`tdd-gate` mode) | MERGED 2026-06-07 — full test-first hard-gate substance preserved as review-gatekeeper's `tdd-gate` mode incl. delete-impl-and-restart-from-failing-test semantics (zero capability loss) | — | — | — | superseded by agent:review-gatekeeper row |
| agent:tech-critic-lead | CORE | Critical-CTO contract gate — rejects evidence-free / over-scoped / cheaper-alternative contracts before any worker dispatch | ①④ | contract JSON + spec excerpt → YAML APPROVE/REJECT + reject_reason + improvement_path | PM sends every phase contract through it before fan-out (pm-orchestrator.md hard rule 4) | All other review agents gate diffs post-implementation; this gates contracts pre-implementation — unique position in the loop |
| agent:vault-pm-orchestrator | KEEP | Vault-domain PM — ticket-board loop driving a vault to rubric pass via parallel worker dispatch + verify + adversarial audit | ③ | fix-plan.json / score-state.json → worker tickets, verified deliverables, pm-round-N.md reports, rubric-pass vault | /vault-build dispatches it after Phase 4; resume on stalled state files (its :276 /nbm-to-obsidian trigger is stale) | Distinct from agent:pm-orchestrator (code loop vs vault loop, different state/ticket systems); only orchestrator for the vault pipeline |
| agent:wiki-stub-expander | INTEGRATE | Vault stub enricher — expands 1-2-source wiki articles to ≥3 sources + Relationships section from graph.json | ③ | stub wiki pages + graph.json → rewritten articles (Summary/Sources/Relationships/Audit Trail) | vault-pm-orchestrator ticket for wiki_articles dim (expansion direction); auto-planned via vault/sources/notebooklm.py WORKER_FOR_DIM | NOT overlap-free — twin of stub-merger on the same wiki_articles dimension (expand vs merge) → vault-structure-curator |
| agent:worker | CORE | Single-contract scope-bound implementation worker — edits, runs verify, reports hashed-evidence diff; never self-reviews | ① | contract/ticket + spec section + verify checklist → in-scope diff + hashed verify log + status.json/exit-code/DONE marker | PM dispatches per contract as general-purpose+sonnet with TICKET=<path> boot (pm-orchestrator.md worker dispatch template) | Only implementation-role contract in the auto-pilot loop; swarm worker prompts live in swarm/scripts/prompts (separate pipeline); reviewers are read-only by rule 8 so no role bleed |
| codex-skill:codex-orchestra | KEEP | Codex-seat conductor: Opus/Claude as PM+review gate, Codex as implementation worker | ① | repo task → Opus PM plan (claude -p plan mode) → Codex implementation → Opus APPROVED/NEEDS_REVISION/FAILED verdict | $codex-orchestra / /codex-orchestra typed inside a Codex CLI session | skills/codex-orchestra (Claude side, round-0 CORE) is the opposite-seat sibling — codex/README.md:48-50 declares them intentionally separate, not duplicates; no other Codex-side asset runs the dual-review loop |
| codex-skill:diagnose | KEEP | Disciplined reproduce→hypothesize→instrument→fix→regression-test loop for hard bugs/perf on Codex seat | ① | bug/perf symptom → deterministic repro loop + ranked hypotheses → smallest fix + regression test + verification output | $diagnose in Codex when debugging failures/flaky tests/slow paths | superpowers:systematic-debugging covers the Claude seat only; this skill names it and yields discipline to it — complementary cross-runtime, no Codex-side alternative |
| codex-skill:grill-with-docs | KEEP | Stress-test a plan against repo domain model/CONTEXT.md/ADRs; capture decisions as ADRs at design time | ② | plan + repo vocabulary → strongest challenges, terminology fixes, proposed/applied CONTEXT.md+ADR edits | $grill-with-docs in Codex when user wants a design grilled or domain docs sharpened | partial ADR-authoring overlap with vault-knowledge-author (vault loop, Claude) and doc-management intent layer — but those are maintenance/vault flows; interactive design-time grilling on the Codex seat exists nowhere else. |
| codex-skill:handoff | KEEP | Compact current Codex conversation into a continuation document for a fresh agent/session | ③ | live session state → /private/tmp/codex-handoff-*.md (objective, decisions, commands, pending, blockers) + top next action | $handoff in Codex when user asks for handoff/session summary/next-agent briefing | gstack context-save and agent:retro persist on the Claude side; nothing else produces Codex-session handoffs — cross-runtime, no duplicate |
| codex-skill:improve-codebase-architecture | KEEP | Evidence-backed module-deepening / boundary analysis on the Codex seat (analysis-only by default) | ① | codebase + CONTEXT.md/ADRs → ranked deepening opportunities with evidence paths, migration shape, non-goals (+optional HTML report) | $improve-codebase-architecture in Codex for architecture review/refactor planning | OVERLAPS skills/improve-codebase-architecture (Claude side) in method, but Codex cannot load Claude plugin skills — the fork is the only delivery path; content drift between siblings is the real cost |
| codex-skill:migrate-to-codex | KEEP | Migrate Claude Code instructions/skills/agents/MCP/hooks into Codex project+global config (loop-enabler) | ① | .claude//.mcp.json/CLAUDE.md → AGENTS.md, .agents/skills/, .codex/config.toml+hooks.json+agents/ + migration report table | $migrate-to-codex in Codex; runs scripts/migrate-to-codex.py with scan/plan/doctor/dry-run/validate flags | nothing else in the plugin or globals does Claude→Codex config migration; zero overlap |
| codex-skill:prototype | KEEP | Build disposable logic/UI/interaction prototypes to answer a design question before production code | ① | design question → smallest runnable artifact in /private/tmp (or marked repo path) → proven/disproven + cleanup status | $prototype in Codex when exploring behavior/UI pre-production | no other asset (either runtime) covers throwaway prototyping; superpowers brainstorming/TDD are different phases |
| codex-skill:tdd | KEEP | Red-green-refactor worker discipline, one vertical behavior slice at a time, on the Codex seat | ① | feature/bugfix request → per-slice report: behavior, public seam, RED cmd+failure, GREEN cmd+evidence, refactors, untested residue | $tdd in Codex when user asks for TDD/test-first | agent:tdd-enforcer gates the Claude loop; superpowers:test-driven-development is Claude-global — neither reaches a Codex session; explicit deference clause prevents double-discipline conflicts |
| codex-skill:to-issues | KEEP | Slice a plan/spec/PRD into independently shippable vertical issue tickets (HITL/AFK typed, dependency-ordered) | ① | plan/spec/PRD → numbered breakdown + issue drafts (What to build/Acceptance criteria/Blocked by) + coverage check; gh publish only on explicit ask | $to-issues in Codex when user wants work sliced into tickets | auto-pilot loop generates internal contracts, not human/GitHub issues; no other asset produces vertical issue tickets |
| codex-skill:to-prd | KEEP | Synthesize current conversation into a concise implementable PRD, optionally filed as a GitHub issue | ① | conversation+repo context → PRD (problem/goals/non-goals/requirements/acceptance/risks) + recommended title; gh issue only on explicit ask | $to-prd in Codex when user wants requirements captured | no other asset converts conversation→PRD; superpowers:brainstorming (Claude) is pre-design exploration, different phase and runtime |
| codex-skill:triage | KEEP | Triage bugs/requests/backlogs through a needs-triage→needs-info/ready-for-agent/ready-for-human/wontfix state machine | ① | raw issues/reports → per-item state, type, severity, priority, confidence, owner, evidence, next action; remote mutations only on explicit ask | $triage in Codex for backlog sorting/issue triage | nothing else in plugin or globals does issue triage; loop reviewers judge diffs, not backlogs |
| codex-skill:zoom-out | KEEP | Explain the bigger-picture system map around a local code area/plan/decision before acting | ① | file/module/plan under discussion → system map (responsibility, callers, dependencies, data flow, failure modes) + recommended next moves | $zoom-out in Codex when user asks for broader context | OVERLAPS graphify query/explain conceptually, but graphify is Claude-side + requires graphify-out/; zoom-out is the only Codex-runtime equivalent — cross-runtime, tolerated |
| command:auto-pilot | CORE | Interactive entry + preflight contract for the auto-pilot skill (PM-worker-dual-review loop) | ① | spec + git state → preflighted skill dispatch → PM loop driving .planning/auto-pilot/state.json | /auto-pilot start\|status\|resume\|stop in the main session (Opus PM) | skill:auto-pilot owns loop logic; this command adds preflight/probe gates the skill does not duplicate — wrapper, but with gate logic existing nowhere else |
| command:auto-pilot-server | CORE | Launch the headless infinite auto-pilot loop driver (claude -p per phase, HEAD snapshot + rollback) | ① | .planning/auto-pilot/state.json + spec → background headless-loop.py process + iter logs + terminal state | /auto-pilot-server slash command; scripts/evals/runner.py and dogfood_tier1/2.sh reuse the driver | Only headless entry. /auto-pilot is the in-conversation counterpart — intentional headless/interactive split, no third asset spawns claude -p loops |
| command:eval-run | CORE | Run cut-1 evals harness in advisory mode (clone-per-case, deterministic oracle, pass-rate vs blessed baseline) | ① | eval cases (--tier smoke\|full) → advisory pass-rate report vs baseline (never blocks) | /eval-run slash; CI-adjacent advisory ritual | Sole loop-deliverable evals entry; swarm-bench benchmarks swarm topologies not auto-pilot outputs |
| command:harness-build | INTEGRATE | Dispatch harness-generator to implement the next unfinished sprint with evaluator-negotiated contract | ① | spec.md + progress.json + budget.json → sprint contract + implementation + handoff doc | /harness-build (frontmatter agent: harness-generator, context: fork) | OVERLAPS planned /harness routing command — becomes `build` subcommand |
| command:harness-drift | INTEGRATE | Run 6-check harness drift scanner (pointers, hook scripts, MCP↔deps, forbidden patterns, ADR, security baseline) | ②④ | project → 6-check itemized drift report, exit 0/1 | /harness-drift — pre-commit, weekly, pre-release | Harness-config drift ≠ doc-management semantic doc drift (different object), but command shell overlaps planned /harness-ops — becomes `drift` subcommand |
| command:harness-loop | INTEGRATE | Autonomous score→fix lowest dim→rescore loop across 14 dims until target (default 95, max 15 iters) | ①④ | [target] [max] → iterated autofixes + .claude/score.json, exit 0/1/2(stuck) | /harness-loop [target] [max] | OVERLAPS planned /harness-ops — becomes `loop` subcommand; loop script itself lives in skill:setup-harness |
| command:harness-plan | INTEGRATE | Dispatch harness-planner to expand a 1-4 sentence prompt into .claude/harness/spec.md | ① | short product prompt → .claude/harness/spec.md + sprint breakdown table | /harness-plan <desc> (frontmatter agent: harness-planner, context: fork) | OVERLAPS planned /harness routing command — per CEO decision this becomes the `plan` subcommand |
| command:harness-qa | INTEGRATE | Dispatch harness-evaluator for sprint contract sign-off (A) or implementation QA with write-set compliance (B) | ①④ | sprint NN → sprints/NN-eval.md + progress.json (completed\|needs_revision) | /harness-qa <sprint-number> (frontmatter agent: harness-evaluator) | OVERLAPS planned /harness routing command — becomes `qa` subcommand |
| command:harness-score | INTEGRATE | Score project harness across 14 dimensions, write .claude/score.json | ④ | project → .claude/score.json + per-dim summary | /harness-score | OVERLAPS planned /harness-ops — becomes `score` subcommand; also consumed by harness-loop internally |
| command:harness-setup | INTEGRATE | Run setup-harness bootstrap (stack detect, 13 hook scripts, settings dedupe-merge, CLAUDE.md scaffold) | ④ | project tree → .claude/scripts + settings.local.json + CLAUDE.md + ADR 0001 + .gitignore updates | /harness-setup [--dry-run] | OVERLAPS skill:setup-harness (owns the script) and planned /harness-ops — thin shell, becomes `setup` subcommand |
| command:harness-verify | INTEGRATE | Run 7 functional hook tests + A-G checks independent of the scorer | ④ | project → A-G verification report, exit 0/1 | /harness-verify — post-setup, post-loop | OVERLAPS planned /harness-ops — becomes `verify` subcommand; distinct from harness-score (functional vs static) |
| command:nbm-to-obsidian | REMOVE | Back-compat forwarder to /vault-build --source notebooklm | NONE | vault path → forwarded /vault-build invocation (100% pass-through) | /nbm-to-obsidian (legacy users of notebooklm-vault-builder v0.6.0 only) | FULLY OVERLAPS command:vault-build (--source notebooklm flag) — zero unique behavior |
| command:quality-loop | REMOVE | Arg-parse wrapper delegating to adversarial-review-loop codebase mode | ① | flags → Skill(auto-pilot:adversarial-review-loop, mode=codebase ...) | /quality-loop — but natural-language skill triggers already cover every entry path | FULLY OVERLAPS skill:adversarial-review-loop codebase mode (its own <note> at commands/quality-loop.md:44 admits the state machine moved there) |
| command:setup-claude-md | KEEP | Build harness-style CLAUDE.md system: root ≤50-line pointer + folder-level prohibition-only files mined from incident history | ②④ | repo structure + git incident history → root + folder CLAUDE.md with reason-backed prohibitions + line-count verification | /setup-claude-md on a new or under-documented project | skill:setup-harness scaffolds a skeleton CLAUDE.md but does NOT mine git history for incident-based prohibitions — complementary depth, not duplication |
| command:sha-deploy-init | KEEP | Generate SHA-pinned ci.yml deploy workflow from stack-detected template with secret/placeholder substitution | ④ | project type + gh secret list + ecosystem config → .github/workflows/ci.yml (validated YAML) | /sha-deploy-init [type] when wiring CI/CD for a new project; pairs with sha-deploy-standard skill | skill:sha-deploy-standard = the pattern/contract doc; this = the only generator. Complementary pair, nothing else writes ci.yml |
| command:vault-audit | INTEGRATE | Dispatch adversarial-auditor for independent strict vault re-score | ③ | vault path → meta/audit-rN.md + verdict vs internal score | /vault-audit [path] after vault build/score | OVERLAPS command:vault-score (same scoring surface, adversarial flavor) — per CEO becomes an `--audit` mode of vault-score |
| command:vault-build | KEEP | Export pipeline: build/update Obsidian vault + NotebookLM notebook + graphify output (+ bases/canvas), upsert semantics | ③ | project → up to 5 export destinations + .vault-builder/export-report.json + state.json | /vault-build [flags]; nbm-to-obsidian forwards here; vault-restructure phase 6 shells to `claude -p /vault-build` | Only repo→knowledge-base export asset; repo-docs fixing explicitly delegated to skill:doc-management (gated behind --fix-repo-docs opt-in), so no doc-management overlap in the default flow |
| command:vault-content-verify | INTEGRATE | Dispatch content-fact-checker for sample-based factual grounding audit of vault edges/concepts/ADRs | ③ | vault path → meta/content-audit-rN.md + edge_fact/concept/ADR accuracy % | /vault-content-verify [path] | OVERLAPS command:vault-score content axis (score_content.py) — per CEO becomes a `--content-verify` mode of vault-score |
| command:vault-dashboard | KEEP | Generate + open static HTML dashboard for a vault (structural/content scores, tickets, cost telemetry, audit trail, PM rounds) | ③ | vault path → dashboard/data.json + browser open of dashboard/index.html | /vault-dashboard [path] [--no-open] after build/score/audit runs | Only visualization surface in the plugin — nothing else renders scores/telemetry to HTML |
| command:vault-drift | INTEGRATE | Read-only repo code↔docs drift report (gap/orphan/symbol/claim) — superseded for repo docs by doc-management AUDIT | ② | repo path → drift-report.md/json, 4 drift types, no mutations | /vault-drift <repo> — but its own doc routes users away for the primary use case | OVERLAPS skill:doc-management AUDIT mode (stale-doc detection on a repo's own docs) — the exact duplication DOC-CORE supremacy targets |
| command:vault-restructure | INTEGRATE | One-shot autonomous Obsidian directory restructure: backup, rename, merge, NotebookLM split, skeletons, build manifests; idempotent via state file | ③ | ~/Documents/Obsidian + _mapping.py → restructured <project>-Vault dirs + manifests + ~/.claude/state/obsidian-restructure-state.json | /vault-restructure [--dry-run\|--phase N\|--reset\|--execute-builds] | OVERLAPS command:vault-build target-layout responsibilities (phase 6 literally shells to /vault-build) — per CEO folds in as a vault-build flag |
| command:vault-resume | INTEGRATE | Resume vault PM ticket loop from last ticket-state.json checkpoint | ③ | ticket-state.json → re-dispatched pending tickets via vault-pm-orchestrator, or final report if ≥95 | /vault-resume [path] after interrupted PM loop | OVERLAPS command:vault-build loop lifecycle — per CEO becomes `--resume` flag on vault-build |
| command:vault-score | KEEP | Structural + content scoring of an existing vault, no PM loop (post-consolidation: + audit + content-verify modes) | ③ | vault path → structural score + content score + top gaps (read-only, no dispatch) | /vault-score [path] after manual edits or builds; audit/content-verify fold in as modes | Post-consolidation it IS the single vault quality surface; distinct from quality-eval (code rubric) and harness-score (harness config) |
| command:vault-selftest | KEEP | Plugin internal consistency check: manifest, marketplace, agent frontmatter, script syntax, rubric, hooks, MCP config, command frontmatter | ④ | plugin tree → pass/fail report (8 check classes), exit 0/1, optional --json for CI | /vault-selftest [--json] before publishing or after significant plugin changes | Only plugin-self validation asset; harness-verify tests a TARGET project's harness, this tests the plugin itself |
| hook:codex-conductor-guard.py | CORE | Deny Claude code/test writes in repos opted into codex-orchestra conductor mode (.codex-conductor marker) | ①④ | PreToolUse JSON payload (Edit/Write/MultiEdit/NotebookEdit) → permissionDecision allow/deny JSON | hooks.json PreToolUse matcher Edit\|Write\|MultiEdit\|NotebookEdit, python3, timeout 5 | pre-reviewer-write.sh gates by subagent ROLE env; this gates by repo MARKER — orthogonal axes, no other asset enforces conductor delegation |
| hook:doc-sync-update.sh | CORE | Merged graph-freshness watcher: marks code graph stale on code edits AND runs eager `graphify update <dir>` on vault raw/sources .md changes; feeds doc-management MAINTAIN | ② | PostToolUse Write/Edit/MultiEdit payload → code branch: touch graphify-out/needs_update (+ eager GRAPHIFY_AUTOSYNC=1); vault branch: synchronous `graphify update <dir>` on raw/sources .md edits (GRAPHIFY_VAULT_AUTOSYNC=0 to disable) | hooks.json PostToolUse matcher Write\|Edit\|MultiEdit (single entry, merged from two) | graphify_update.sh absorbed INTO this hook (2026-06-06, round-2 W2 δ) — vault md branch now inside doc-sync-update.sh; single PostToolUse entry covers both path classes |
| hook:guard-destructive.py | CORE | Block unverified destructive Bash (rm -rf, force-push, hard reset, DROP/TRUNCATE/DELETE-no-WHERE, manual ssh/scp) with hourly evidence-marker override | ④ | PreToolUse Bash payload → permissionDecision allow/deny JSON | hooks.json PreToolUse matcher Bash, python3, timeout 5 | pre-bash-guard.sh covers non-destructive friction patterns (TUI/ruff/SSL); notebooklm_delete_gate covers NotebookLM domain incl. MCP shape this hook can't see — domains disjoint |
| hook:notebooklm_delete_gate.sh | KEEP | Deny notebooklm notebook/source deletes (CLI and MCP) unless NBM_DELETE_CONFIRMED=1 | ③④ | PreToolUse payload (Bash command or mcp__notebooklm__*delete* tool_name) → deny JSON / silent allow | hooks.json PreToolUse, two entries: matcher Bash + matcher mcp__notebooklm__.*delete.* | guard-destructive.py only inspects Bash tool_input.command — it cannot gate MCP tool calls; this is the sole MCP-shape delete gate |
| hook:pm_final_report.sh | KEEP | Emit PM loop final report (scores + ticket stats) on session Stop when a vault PM loop was active in last 24h | ③ | Stop event + $NBM_VAULT_PATH/meta/ticket-state.json → meta/pm-final-report-<ts>.md + systemMessage | hooks.json Stop matcher * | vault-pm-orchestrator writes in-loop state but nothing else fires at session Stop; hook is the only interrupt-safe report path |
| hook:post-deploy-verify.sh | KEEP | Detect zombie port listeners and placeholder env values immediately after deploy-class Bash commands | ④ | PostToolUse Bash payload → stderr warnings (never blocks) | hooks.json PostToolUse matcher Bash | Only post-deploy environmental check in the plugin; sha-deploy-standard skill defines pipeline policy, not runtime smoke — complementary |
| hook:pre-bash-guard.sh | CORE | Block known-bad Bash friction patterns before execution (TUI hang / re-export corruption / SSL outage sequences) | ④ | PreToolUse Bash payload → exit 2 deny with stderr reason / exit 0 | hooks.json PreToolUse matcher Bash | Mechanism-level overlap with guard-destructive.py (both Bash deny guards, merge candidate to cut per-call spawns) but pattern domains are disjoint: friction/incident patterns vs destructive deletes; pre-edit-composition- |
| hook:pre-edit-composition-root.sh | CORE | Block direct edits to non-trivial composition roots/re-export modules unless explicitly forced | ④ | PreToolUse Edit/Write/MultiEdit payload → exit 2 deny with remediation text / exit 0 | hooks.json PreToolUse matcher Edit\|Write\|MultiEdit | Only file-content-aware edit guard; pre-bash-guard rule 2 covers the Bash(ruff) vector of the same incident — different tool surface, no duplication |
| hook:pre-reviewer-write.sh | CORE | Confine reviewer-role subagents to their output dir and read-only operations during the dual-review loop | ①④ | PreToolUse payload + AUTO_PILOT_SUBAGENT_ROLE/AUTO_PILOT_OUTPUT_DIR env → exit 2 deny / exit 0 | hooks.json PreToolUse matcher Edit\|Write\|MultiEdit\|Bash (active only when role env set by PM dispatch) | Sole role-scoped sandbox enforcement; codex-conductor-guard gates by repo marker not role; nothing else makes reviewer no-mutation a hard guarantee |
| hook:preflight-path.sh | KEEP | Validate working directory and surface mid-loop auto-pilot state at session start | ①④ | SessionStart (no payload deps, reads cwd + .planning/auto-pilot/state.json) → stderr warnings, always exit 0 | hooks.json SessionStart matcher * | Only SessionStart hook in the plugin; /auto-pilot command preflight runs at command time, not session start — different moment, complementary |
| skill-shell:codebase-perfection-loop | DELETED | Directory removed (rubric provenance distilled into quality-eval; 6-gate cleared W2-ε1 2026-06-06) | NONE | n/a — deleted | n/a | Fully superseded: codebase quality loop → adversarial-review-loop, lifecycle → pm-quality-harness-loop, rubric SoT → quality-eval. |
| skill:adversarial-review-loop | CORE | Dual-track Codex×Claude review + 13-dim codebase improvement loop engine (branch/codebase/multi-agent 3 modes) | ① | branch-PR diff or repo tree → both-approve verdict / score-state.json + contracts + merged quality/* branches | "adversarial review"/"quality loop"/--multi-agent triggers; composed by pm-quality-harness-loop; /quality-loop command delegates here | Only dual-review + contract-fan-out engine in the plugin. quality-eval supplies the rubric (data, not engine); pm-quality-harness-loop wraps it (sequencing, not review). Supersession of quality-eval as ENTRY POINT is claimed but quality-eval stays the rubric SoT (data vs engine split). |
| skill:auto-pilot | CORE | Self-driving spec-phased PM→tech-critic→worker→dual-review→verify-gate loop (interactive + headless) | ① | spec (docs/specs/*\|SPEC.md) → phased commits with auto-pilot trailers + .planning/auto-pilot/state.json + reports | /auto-pilot start\|status\|resume\|stop; headless via scripts/headless-loop.py through /auto-pilot-server | Only in-session spec-phased loop; swarm is the persistent-tmux alternative backend and the SKILL itself contains the explicit routing table between the two. Friction guards delegate to hooks (preflight-path, pre-edit-composition-root, dispatch-contract-gate). |
| skill:codex-orchestra | CORE | Conductor mode — Claude plans/reviews/gates, Codex (codex-companion task --write) authors all source/test code; opt-in hard enforcement via .codex-conductor marker | ①④ | task → plans/<task>-plan.md → Codex workspace-write diffs → APPROVED/NEEDS_REVISION/FAILED gate → user commit | explicit /codex-orchestra or "codex orchestra" only (default OFF, no NL auto-trigger) | Only asset that inverts authorship (Codex writes, Claude never edits source). ARL uses Codex as reviewer/eval/worker inside its own loop — different role split. Paired guard hook codex-conductor-guard.py is registered in hooks.json. |
| skill:diagnosing-llm-output-leaks | KEEP | Triage map: classify LLM output token leaks as data-layer / prompt / stochastic / bilingual-gap before iterating prompts a 3rd time | ① | N≥10 fresh samples + pattern-scanner aggregates → leak class + correct fix surface (builder vs prompt vs ignore) | manual, when prompt-level leak fixes are not converging; no hook/script wiring | Only LLM-leak triage asset; diagnosing-stale-runtime covers runtime-vs-source mismatch — disjoint symptom class. quality-eval dim 12 scores prompt quality but does not diagnose leaks. |
| skill:diagnosing-stale-runtime | KEEP | Runtime-vs-source mismatch checklist: dead-or-alive verdicts, stale caches/processes, stub-vs-delete, two-writer races, N=1 smoke noise | ① | symptom (removed code still runs / restart didn't fix / prompt change not taking) → ordered evidence checks → verified verdict | manual, before claiming code dead/alive or trusting handoff claims; complements residue-audit's 6-gate | residue-audit owns deletion procedure (6-gate); this owns runtime-state diagnosis — adjacent, explicitly different surfaces. No other asset covers lru_cache/PM2-respawn/flock race classes. |
| skill:doc-management | CORE | THE docs subsystem — REBUILD (graph-native demolish+reauthor) / MAINTAIN (SHA-freshness targeted refresh) / AUDIT (read-only evidence fan-out) keeping repo docs matching code | ② | repo + code-only graphify graph → rebuilt/refreshed/audited doc tree + installed L2 guard (check-doc-reference-integrity.mjs) + L3 freshness (check_design_doc_freshness.py) | doc rot/sync/drift/audit phrasings; MAINTAIN fed by hooks/doc-sync-update.sh PostToolUse watcher (registered in hooks.json) | Doc-core supremacy holder — vault drift trio / docs-worker / docs-verifier already CEO-REMOVEd in its favor. vault-build owns export only; residue-audit owns code-level comments; this owns doc prose + structure. claim-ledger approach was NOT adopted (historical note only). |
| skill:improve-codebase-architecture | KEEP | Interactive deep-module discovery — deletion-test/seam vocabulary, candidate list → grilling loop, with CONTEXT.md glossary + ADR side effects | ①③ | codebase + CONTEXT.md + docs/adr/ → numbered deepening candidates → grilled design + ADR/glossary updates | "improve architecture"/refactoring-opportunity phrasings; manual, conversational | ARL architecture dim scores/fixes mechanically against a rubric; this is exploratory design conversation producing ADRs — different mode and output. Companion LANGUAGE.md / INTERFACE-DESIGN.md / DEEPENING.md all exist. |
| skill:pm-quality-harness-loop | KEEP | Superset lifecycle orchestrator: CI-gate-faithfulness-first → dim fan-out → adversarial bug-hunt → harness-doc sync → honest DoD → autonomous merge tail | ①②④ | repo → measurably-better + correctness-verified + docs-synced + merged repo, with real (non-round-number) weighted score | "big-tech 95"/"quality lift then merge"/"harden and ship" phrasings; explicitly composes ARL + quality-eval + doc-management | Cites-not-duplicates the ARL engine; unique value = sequencing + Phase 0 CI-faithfulness + Phase 3 harness-doc-sync + merge tail, none of which ARL has. Thin orchestration layer → uniqueness 7. |
| skill:quality-eval | CORE | 13-dimension big-tech rubric SoT — weights, hard-fail caps, penalty calibration, anti-inflation guard, score-state.json schema | ①④ | any codebase (auto language detect / .quality-loop.json) → per-dim scores + weighted total → .planning/quality/score-state.json + eval-report | standalone "quality eval"/"score this project"; consumed by ARL codebase EVALUATE (../quality-eval/SKILL.md fed to Codex track), pm-quality-harness-loop Phase 2, swarm-bench per-arm scoring | Sole rubric definition — ARL/pm-loop/swarm-bench all cite it rather than restating dims. Its standalone TRIGGER surface collides with ARL codebase-mode triggers (ARL claims to supersede it as entry) — keep as rubric SoT, |
| skill:residue-audit | KEEP | Semantic code-residue audit linters can't block on — dead code / diverged duplicates / stale comments / orphan artifacts via parallel read-only fan-out + 6-gate + FP catalog, report-first | ①④ | green-baseline repo + deterministic-tool seed (ruff/vulture/jscpd) → ranked P0/P1/P2 table with 6-gate column + FALSE-POSITIVE(keep) list → approval-gated category-batched PRs | "residue audit"/"dead code purge"/"잔재 제거" phrasings; post-refactor/migration | Only semantic dead-code/dedup asset; explicitly fences off doc-management AUDIT (doc prose), ARL (quality scoring), setup-harness (bootstrap). references/six-gate.md + false-positive-catalog.md both exist. |
| skill:setup-harness | CORE | Full harness bootstrap/audit — pointer CLAUDE.md + 8 hooks/6 events + drift detection + MCP + sandbox + autonomous 15-dim score→loop→verify orchestrator | ④ (primary) + ① (harness-loop orchestrator) | any project → installed .claude harness (scripts/hooks/CLAUDE.md/ADR/.gitignore) + .claude/score.json + verify report | "harness setup"/"하네스 세팅"/drift-check phrasings; bootstrap.sh / harness-loop.sh direct; /harness-* commands (CEO: 8→2 routing) | Only harness bootstrapper; doc-management owns doc content, this owns enforcement scaffolding. 30 bundled scripts + own bats tests (harness-cli.bats, harness-hooks.bats) + templates + own evals dir. |
| skill:sha-deploy-standard | KEEP | SHA-pinned CI/CD deploy standard — single ci.yml, workflow_dispatch target_sha rollback, secrets-via-envs, GH_TOKEN cleanup trap, audit trail | ④ | project + gh secrets → ci.yml from 4 templates (python-pm2/nextjs-pm2/nextjs-artifact/static-rsync) + .deploy/ audit trail | "SHA deploy"/"ci.yml"/"deploy workflow" phrasings; applied via /auto-pilot:sha-deploy-init command | Only deploy-pipeline asset in the plugin; setup-harness covers dev-time hooks not CI/CD topology. All 4 templates verified present at deploy/templates/. |
| skill:swarm | KEEP | Launch persistent tmux multi-agent pool — 1 Opus PM pane + 4-10 claude/codex worker panes on per-worker git worktrees with file-based ticket bus | ① | .planning/autopilot/config.json → live tmux session (autopilot-<basename>) + N worktrees/branches + inbox/in_progress/done/scores bus | /swarm, "start the swarm" (+ legacy /autopilot-swarm aliases); cross-referenced as alternative backend by auto-pilot SKILL routing table | Only persistent, session-surviving execution backend; auto-pilot skill = in-session spec-phased counterpart with explicit when-to-prefer table. CEO 6→2: absorbs init/status/stop/ticket — consolidation executed W2-ⓑ: 5 swarm skills merged into this one (subcommand routing). |
| skill:swarm-bench | KEEP | 3-arm benchmark — same task via full swarm vs claude-solo vs codex-solo, each scored by quality-eval, comparison report | ① | task description (+--repeats N) → .planning/autopilot/bench/<epoch>/report.md with score/time/tokens per arm | "swarm bench"/"is swarm better than codex"; runs swarm/scripts/bench.sh | Only empirical-comparison asset (CEO fixed: stays separate from swarm). Reuses quality-eval rubric — consumer, not duplicator. bench.sh verified present. |
| skill:swarm-init | INTEGRATE | NL/Q&A wizard writing .planning/autopilot/config.json (worker count/models/roles/goal, normalization table) | ① | NL config prompt or AskUserQuestion → validated config.json (atomic write) | "swarm init"/"configure swarm"; invoked by swarm skill step 1 when config missing | OVERLAPS swarm lifecycle — pure sub-step of launch (CEO fixed: integrate into consolidated swarm skill as init flow/flag) |
| skill:swarm-status | INTEGRATE | Read-only swarm diagnostics — panes, queue depth, ledger weights, recent scores, pm.log tail; escalates to swarm-monitor agent | ① | tmux session + .planning/autopilot/ bus → markdown status report (+warnings on stale tickets / weight<0.7) | "swarm status"/"how is autopilot doing" | OVERLAPS swarm lifecycle — status sub-command (CEO fixed: integrate into swarm). swarm-monitor AGENT stays per CEO (agents 3 KEEP) |
| skill:swarm-stop | INTEGRATE | Graceful swarm shutdown via swarm/scripts/stop.sh (+--purge worktree/branch removal with confirm) | ① | session name → killed tmux session (+optional purged worker worktrees/branches), .planning/autopilot/ preserved | "stop swarm"/"kill autopilot" | OVERLAPS swarm lifecycle — stop sub-command (CEO fixed: integrate into swarm); stop.sh verified present |
| skill:swarm-ticket | INTEGRATE | Manual ticket injection into worker-N inbox bypassing PM dispatch (scope/accept flags, atomic JSON write) | ① | <worker-id> <task> [--scope][--accept] → .planning/autopilot/inbox/worker-<id>/T-manual-<epoch>.json | "give swarm a task"/"inject ticket" | OVERLAPS swarm lifecycle — ticket sub-command (CEO fixed: integrate into swarm) |
