{
  "passed": true,
  "evidence": {
    "cmd_output": "All 3 tests passed.\n--- 3 tests passed ---",
    "files_checked": ["swarm/schemas/verify.schema.json", "swarm/tests/verify-schema/test.sh"],
    "diff_sha": "abc1234"
  },
  "reproductions": [
    {"scenario": "missing required field", "cmd": "echo '{}'", "observed": "validation error: 'passed' is required"}
  ],
  "downgraded_to": null,
  "notes": "All acceptance commands re-ran cleanly. No hidden side-effects found. One adversarial probe attempted (empty input) — handled correctly by the schema guard."
}
